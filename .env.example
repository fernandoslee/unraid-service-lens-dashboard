# Unraid server connection (or configure via the Setup page at http://localhost:8080/setup)
UNRAID_HOST=tower.local
UNRAID_API_KEY=your-api-key-here
UNRAID_VERIFY_SSL=false

# Authentication (configured via Setup wizard on first run)
AUTH_ENABLED=false
AUTH_USERNAME=admin
AUTH_PASSWORD=
SESSION_SECRET_KEY=
